package airtravel;

/**
 * Represents classes of seats. Seat classes are ordered from top to bottom by priority,
 * with the top having the highest priority
 */
public enum SeatClass {
    BUSINESS,
    PREMIUM_ECONONMY,
    ECONOMY,
}
